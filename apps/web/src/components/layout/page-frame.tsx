export function PageFrame({ title, eyebrow, description, actions, children }: { title: string; eyebrow: string; description: string; actions?: React.ReactNode; children: React.ReactNode }) {
  return <div className="mx-auto w-full max-w-[1600px] px-4 py-5 sm:px-5"><div className="mb-5 flex flex-col gap-3 border-b border-[var(--border-subtle)] pb-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="label-caps mb-1.5">{eyebrow}</p><h1 className="text-[22px] font-semibold leading-7 tracking-[-.025em]">{title}</h1><p className="mt-1 max-w-3xl text-[13px] leading-5 text-[var(--text-secondary)]">{description}</p></div>{actions}</div>{children}<p className="mt-5 border-t border-[var(--border-subtle)] pt-3 text-[10px] text-[var(--text-muted)]">分析用于学习决策参考，不代表录取概率。</p></div>;
}
