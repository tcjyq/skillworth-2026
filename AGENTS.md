# SkillWorth Live — 工程协作约定

## 项目定位

技值 SkillWorth（开发代号：SkillWorth Live）是面向中国技术岗位招聘市场的劳动力市场分析与技能学习决策平台。系统的核心问题是：给定目标岗位、用户现有技能和学习时间预算，下一项最值得学习的技术是什么。

本仓库当前处于规格与架构阶段。实现必须以 `docs/PRD.md`、`docs/ARCHITECTURE.md`、`docs/METHODOLOGY.md`、`docs/DATA_SOURCES.md` 和 `docs/DATA_DICTIONARY.md` 为准；需求、字段或方法变更时，先更新对应文档，再修改代码。

## 强制工程原则

- 使用 UTF-8；面向项目的文档与默认回复使用中文。
- 不为演示效果硬编码指标、排名、图表数字或推荐结果。所有界面数值必须来自可重复执行的计算。
- 每个指标必须有定义、分母、过滤条件、时间窗口、适用范围和已知局限，并同步更新 `docs/METHODOLOGY.md`；字段含义、枚举和空值语义同步更新 `docs/DATA_DICTIONARY.md`。
- 原始岗位记录必须保留来源和 provenance。不得覆盖原始数据，不得删除跨平台来源映射。
- 未获得明确合法授权的数据源必须保持 disabled；只实现手动导入或授权 Connector 的接口。
- 严禁绕过验证码、登录、反爬、限流、付费墙或其他访问控制；不得提交此类实现或说明。
- LLM 不得执行统计计算、评分或优化。它只能作为可选的文本解释层，或在可审计的规则提取失败后作为技能抽取候选来源。
- 优先完成数据质量、分析正确性和测试，再实现前端展示与动效。
- 不修改当前任务无关的文件；不提交原始招聘数据、个人信息、密钥或令牌。

## 目录边界

| 路径 | 职责 |
| --- | --- |
| `apps/api` | FastAPI 路由、Pydantic schema、服务编排；不承载指标公式。 |
| `apps/web` | Next.js 页面与数据展示；不重复计算后端指标。 |
| `packages/connectors` | 手动导入和授权数据 Connector；默认拒绝未授权连接。 |
| `packages/data-pipeline` | Bronze → Silver → Gold 转换、标准化和质量校验。 |
| `packages/analytics` | 可测试的指标、统计模型、图网络和优化算法。 |
| `packages/contracts` | API、数据模型和共享枚举的契约定义。 |
| `packages/ui` | 可复用的 Web UI 组件和设计 token。 |
| `data` | 本地数据分层；原始与处理后的真实数据不进入版本控制。 |
| `docs` | 产品、架构、方法论与数据来源的唯一书面依据。 |

## 实现与测试要求

- 每个分析模块至少新增单元测试；数据管道新增 fixture 驱动的集成测试；重要用户路径新增 Playwright 端到端测试。
- 任何会影响指标结果的变更，必须同时更新方法论、测试样例和版本说明。
- 新依赖、公共 API 变更、数据模型变更和 Connector 启用必须先更新架构或数据来源文档。
- 使用明确的输入和输出模型；统计函数不得直接依赖 HTTP 请求、页面状态或未版本化全局配置。
- Demo 数据可以进入 `data/demo`，但必须明确标记为公开、合成或匿名化样例，并通过同一管道计算。

## 数据与隐私

- `data/raw`、`data/bronze`、`data/silver`、`data/gold` 只存放本地或受控数据，不提交真实数据文件。
- 原始职位描述、公司联系人、候选人信息及任何可识别个人信息默认不展示、不导出。
- 每次导入都要记录 connector、授权状态、导入时间、原始文件哈希、原始记录标识和处理版本。

## 提交前检查

- 检查改动没有混入无关重构、依赖锁文件噪声或机密信息。
- 运行与改动范围相应的测试；没有可运行测试时，在提交说明中明确原因。
- 检查 Dashboard 新增字段是否可以追溯到 Gold 数据或 analytics 输出。
- 检查新增指标是否已写入 `docs/METHODOLOGY.md`。
